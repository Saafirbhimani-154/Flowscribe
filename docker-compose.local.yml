version: '3.8'
services:
  frontend:
    env_file: .env.local
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: builder
    ports:
      - '7000:7000'
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=7000
      - CHOKIDAR_USEPOLLING=true
    command: npm run dev -- --host --port 7000

  backend:
    env_file: ./backend/.env.local
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - '5000:5000'
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=5000
    depends_on:
      - db
    command: sh -c "npx tsx src/database/run_migrations.ts && npx tsx src/database/run_seeders.ts && npm run dev"

  db:
    container_name: flowscribe_postgres_local
    env_file: .env.local
    image: postgres:15-alpine
    ports:
      - '54322:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    container_name: flowscribe_redis_local
    env_file: .env.local
    image: redis:7-alpine
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

  pgadmin:
    container_name: flowscribe_pgadmin
    image: dpage/pgadmin4
    env_file: .env.local
    ports:
      - '5050:80'
    depends_on:
      - db
    volumes:
      - pgadmin_data:/var/lib/pgadmin

volumes:
  postgres_data:
  redis_data:
  pgadmin_data:
